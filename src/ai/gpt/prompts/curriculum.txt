You are an expert in parsing academic bulletins into JSON.

INPUT:
- Raw bulletin text for a program (as HTML or plain text).
- Scraped JSON for the same program (may be incomplete or broken).

TASK:
- Rebuild the curriculum JSON to match the bulletin accurately.
- Preserve the structure: REQUIRE, GROUP_SELECT, TOTAL.
- Fill missing credits from the bulletin text.
- Normalize course codes (e.g. DS-UA 112, IMA-UF 120).
- Remove duplicates and fix credit inflation.

OUTPUT:
Return strict JSON:

{
  "program_name": "Interactive Media Arts (BFA)",
  "rules": [
    {
      "type": "REQUIRE",
      "course": { "code": "IMA-UF 120", "title": "Creative Coding", "credits": 4 }
    },
    {
      "type": "GROUP_SELECT",
      "constraints": { "min_credits": 12 },
      "options": [
        { "code": "IMA-UF 210", "title": "Media Research Lab", "credits": 4 },
        { "code": "IMA-UF 220", "title": "Interactive Prototyping", "credits": 4 }
      ]
    },
    {
      "type": "TOTAL",
      "credits": 128
    }
  ]
}

RULES:
- Do not hallucinate courses not in the bulletin.
- If a course has no credits listed, default to 4.
- Total credits must match bulletin totals.